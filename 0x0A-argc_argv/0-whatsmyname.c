#include <stdio.h>
/**
 * main - print its name
 * Return: 1 or 0
 * @argc: argument count
 * @argv: array
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
