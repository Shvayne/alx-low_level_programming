#include "main.h"
/**
 * _islower - uses is lower function
 * to check for lowercase letters
 * Return: 1 for sucess 0 for fai
 * @c: parameter to be checkedl
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
{
return (0);
}
}
